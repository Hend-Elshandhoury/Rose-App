import React from "react";

type NewPasswordStepProps = {
  email: string | null;
};

export default function NewPasswordStep({ email }: NewPasswordStepProps) {
  /* to do function (not ready yet) */
  // const onSubmit = (values) => {
  //   if(!email) return;
  // }
  return <div>NewPasswordStep step</div>;
}
